#ifndef _CONFIG
#define _CONFIG

#include <string>
#include <vector>

using namespace std;

// Global Information
const int PAGE_SIZE = 16384;
const int MAX_ENTRIES = 128;
const int MIN_ENTRIES = 64;

#endif